{
  "shell_cmd": "if [[ -s ~/.rvm/scripts/rvm ]]; then source ~/.rvm/scripts/rvm; elif [[ -s /usr/local/rvm/scripts/rvm ]]; then source /usr/local/rvm/scripts/rvm; fi;./updategem.sh",
  "working_dir": "${project_path:${folder}}"
}
